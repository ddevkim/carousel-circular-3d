---
alwaysApply: true
---

## 🛠 기술 스택

### 핵심 도구

- **패키지 매니저**: pnpm
- **언어**: TypeScript
- **번들러**: tsup
- **버전 관리**: Changesets
- **코드 품질**: Biome (linting + formatting)

### 개발 환경

- **Monorepo**: pnpm workspace
- **프레임워크**: React 18+
- **CI/CD**: GitHub Actions
- **테스트 도구**: Playground (Vite)

---

## 📦 Monorepo 구조

```
carousel-project/
├── packages/
│   └── carousel-circular/          # 배포 패키지
│       ├── src/
│       │   ├── index.ts           # Public API
│       │   ├── CarouselCircular.tsx
│       │   ├── types.ts
│       │   └── utils/
│       │       ├── calculations.ts # 3D 계산 로직
│       │       └── drag.ts        # 드래그 핸들러
│       ├── package.json
│       ├── tsconfig.json
│       └── tsup.config.ts
│
├── playground/                     # 개발 테스트 환경
│   ├── src/
│   │   ├── App.tsx               # 다양한 사용 예시
│   │   └── examples/
│   ├── package.json
│   └── vite.config.ts
│
├── .github/
│   └── workflows/
│       ├── ci.yml                # Lint + Build
│       └── publish.yml           # NPM 자동 배포
│
├── package.json                   # Root workspace
├── pnpm-workspace.yaml
├── tsconfig.base.json
├── biome.json
└── .changeset/                   # Version 관리
```

---

## 🔧 개발 방법론

### 패키지 버전 관리

- `package.json`에 버전 명시 금지
- 항상 `pnpm add <package>` 로 최신 버전 설치
- Changesets로 시맨틱 버저닝 자동화

### 번들링 전략

- `tsup`으로 ESM + CJS 동시 출력
- `react`, `react-dom`을 external로 처리 (peerDependencies)
- Tree-shaking 최적화

### 스타일링 철학

- **최소 강제**: 필수 3D transform 스타일만 인라인
- **className 기반**: 소비자가 자유롭게 커스터마이징
- **기본 스타일 없음**: 레이아웃/색상/테마 강제 금지

---

## 📐 컴포넌트 API 설계

### Props Interface

```typescript
interface CarouselCircularProps {
  // ========== 필수 ==========
  items: CarouselItem[]; // 최대 30개 권장 (초과 시 자동 slice + warning)

  // ========== 3D 레이아웃 ==========
  radius?: number; // 원의 반지름 (기본: 600px, 자동으로 perspective 계산)
  perspective?: number; // 원근 효과 깊이 (기본: radius * 3.33, 최소 radius * 2)
  itemWidth?: number; // 아이템 너비 (기본: 300px)
  itemHeight?: number; // 아이템 높이 (기본: 400px)

  // ========== 드래그 제어 (항상 활성) ==========
  dragSensitivity?: number; // 드래그 민감도 배율 (기본: 1.0)
  enableMomentum?: boolean; // 관성 효과 (기본: true)
  momentumFriction?: number; // 관성 마찰 계수 (기본: 0.95)

  // ========== 자동 회전 (Optional) ==========
  autoRotate?: boolean; // 자동 회전 활성화 (기본: false)
  autoRotateSpeed?: number; // 회전 속도 deg/frame (기본: 0.2)
  autoRotateResumeDelay?: number; // 마우스 leave/관성 종료 후 재개 딜레이 (기본: 3000ms)

  // ========== 시각 효과 ==========
  opacityRange?: [number, number]; // [min, max] (기본: [0.3, 1])
  scaleRange?: [number, number]; // [min, max] (기본: [0.7, 1])

  // ========== 스타일링 ==========
  className?: string; // 컨테이너 className
  itemClassName?: string; // 각 아이템 className

  // ========== 인터랙션 ==========
  onItemClick?: (item: CarouselItem, index: number) => void;

  // ========== 접근성 ==========
  ariaLabel?: string; // 컨테이너 aria-label (기본: "Circular Carousel")
  keyboardRotationStep?: number; // 키보드 방향키 회전량 (기본: 360 / items.length)
}

/**
 * CarouselItem: content 또는 image 중 하나는 필수
 * content가 제공되면 image는 무시됨
 */
type CarouselItem = CarouselItemWithContent | CarouselItemWithImage;

interface CarouselItemBase {
  id: string | number; // 고유 식별자
  alt?: string; // 이미지 alt text 또는 aria-label
  title?: string; // 아이템 제목
  [key: string]: any; // 추가 메타데이터
}

interface CarouselItemWithContent extends CarouselItemBase {
  content: React.ReactNode; // 커스텀 콘텐츠 (우선순위 높음)
  image?: never; // image와 배타적
}

interface CarouselItemWithImage extends CarouselItemBase {
  image: string; // 이미지 URL
  content?: never; // content와 배타적
}
```

---

## 🧮 핵심 로직: 3D 위치 및 시각 효과 계산

### 의사 코드 (Pseudocode)

```
함수: calculateItemTransform(itemIndex, currentRotation, totalItems, radius, opacityRange, scaleRange)
  입력:
    - itemIndex: 아이템 인덱스 (0부터 시작, index 0이 초기 중앙)
    - currentRotation: 현재 carousel 회전 각도 (degree, 양수 = 시계방향)
    - totalItems: 전체 아이템 수
    - radius: 원의 반지름 (px)
    - opacityRange, scaleRange: 시각 효과 범위

  계산:
    anglePerItem = 360 / totalItems
    itemAngle = itemIndex * anglePerItem

    // 정규화된 각도 계산 (0-360 범위)
    totalRotation = currentRotation % 360
    relativeAngle = (itemAngle + totalRotation + 360) % 360

    // 절대 각도 계산 (0-180 범위로 정규화)
    // 180도 이상이면 360에서 빼서 거울 대칭
    normalizedAngle = abs(relativeAngle > 180 ? 360 - relativeAngle : relativeAngle)

  출력:
    {
      transform: `rotateY(${itemAngle}deg) translateZ(${radiusInRem}rem)`,
      opacity: calculateOpacity(normalizedAngle, opacityRange),
      scale: calculateScale(normalizedAngle, scaleRange),
      zIndex: calculateZIndex(itemAngle, currentRotation)
    }

함수: calculateOpacity(normalizedAngle, opacityRange)
  // normalizedAngle: 0-180 범위
  // opacityRange: [min, max] 예: [0.3, 1]

  ratio = normalizedAngle / 180
  opacity = max(opacityRange[0], opacityRange[1] - (ratio * (opacityRange[1] - opacityRange[0])))

  반환: opacity

함수: calculateScale(normalizedAngle, scaleRange)
  // scaleRange: [min, max] 예: [0.7, 1]

  ratio = normalizedAngle / 180
  scale = max(scaleRange[0], scaleRange[1] - (ratio * (scaleRange[1] - scaleRange[0])))

  반환: scale

함수: calculateZIndex(itemAngle, currentRotation)
  // Z축 거리 기반 z-index 계산 (-100 ~ 100 범위로 정규화)
  radian = (itemAngle - currentRotation) * (PI / 180)
  z = cos(radian) * 100  // -100 ~ 100 범위

  반환: round(z)

함수: pxToRem(px)
  // 1rem = 16px 기준
  반환: px / 16
```

### 핵심 원리

1. **원형 배치**: `rotateY(angle) + translateZ(radius)`로 원주 상에 아이템 배치
2. **각도 정규화**: 회전 각도를 0-180 범위로 정규화하여 앞/뒤 대칭 처리
3. **거리 기반 효과**: 정규화된 각도로 opacity/scale 선형 보간
4. **정규화된 z-index**: -100 ~ 100 범위로 제한하여 CSS 호환성 보장
5. **rem 단위**: px props를 받아 내부적으로 rem으로 변환 (1rem = 16px)

---

## 🖱 드래그 핸들러 구현 전략

### 의사 코드

```
상수:
  - MOMENTUM_START_THRESHOLD = 0.5      // 관성 시작 최소 속도
  - MOMENTUM_STOP_THRESHOLD = 0.01      // 관성 종료 속도
  - DRAG_START_DISTANCE = 5             // 클릭과 구분하기 위한 최소 이동 거리 (px)

상태:
  - rotation: 현재 회전 각도 (degree)
  - isDragging: 드래그 중 여부
  - dragStartX: 드래그 시작 X 좌표
  - lastRotation: 드래그 시작 시 회전 각도
  - velocity: 현재 속도 (degree/frame)
  - momentumAnimationId: 관성 애니메이션 requestAnimationFrame ID
  - autoRotateTimeoutId: 자동 회전 재개 타이머 ID

함수: onMouseEnter()
  if (autoRotate):
    자동회전 일시정지

함수: onMouseLeave()
  if (autoRotate && !isDragging && !isMomentumActive):
    autoRotateTimeoutId = setTimeout(() => {
      자동회전 재개
    }, autoRotateResumeDelay)  // 기본 3000ms

함수: onDragStart(event)
  // 관성 애니메이션 중이면 즉시 중단
  if (momentumAnimationId):
    cancelAnimationFrame(momentumAnimationId)
    momentumAnimationId = null

  // 멀티터치는 첫 번째만 추적
  dragStartX = event.clientX 또는 event.touches[0].clientX
  lastRotation = rotation
  velocity = 0
  isDragging = true

함수: onDragMove(event)
  if (!isDragging): 반환

  // 멀티터치는 첫 번째만 추적
  currentX = event.clientX 또는 event.touches[0].clientX
  deltaX = currentX - dragStartX

  // 최소 이동 거리 확인 (클릭 오동작 방지)
  if (abs(deltaX) < DRAG_START_DISTANCE && !hasMoved):
    반환

  hasMoved = true

  // 드래그 거리를 회전 각도로 변환
  // 원의 둘레 기준: 2πr만큼 드래그 = 360도 회전
  circumference = 2 * PI * radius
  deltaRotation = (deltaX / circumference) * 360 * dragSensitivity
  newRotation = lastRotation + deltaRotation

  // 속도 계산 (관성용)
  velocity = deltaRotation - previousDeltaRotation
  previousDeltaRotation = deltaRotation

  rotation = newRotation

함수: onDragEnd()
  isDragging = false
  hasMoved = false

  if (enableMomentum && abs(velocity) > MOMENTUM_START_THRESHOLD):
    startMomentumAnimation()
  else:
    // 관성 없이 종료 시 자동 회전 재개 스케줄
    if (autoRotate):
      autoRotateTimeoutId = setTimeout(() => {
        자동회전 재개
      }, autoRotateResumeDelay)

함수: startMomentumAnimation()
  함수 animateMomentum():
    rotation += velocity
    velocity *= momentumFriction  // 기본: 0.95

    if (abs(velocity) < MOMENTUM_STOP_THRESHOLD):
      // 관성 종료 시 자동 회전 재개 스케줄
      if (autoRotate):
        autoRotateTimeoutId = setTimeout(() => {
          자동회전 재개
        }, autoRotateResumeDelay)
      반환

    momentumAnimationId = requestAnimationFrame(animateMomentum)

  momentumAnimationId = requestAnimationFrame(animateMomentum)

함수: onKeyDown(event)
  if (event.key === 'ArrowLeft'):
    rotation -= keyboardRotationStep  // 기본: 360 / items.length
  else if (event.key === 'ArrowRight'):
    rotation += keyboardRotationStep
```

### 구현 고려사항

- **마우스 + 터치**: 통합 이벤트 핸들러 (첫 번째 터치만 추적)
- **멀티터치 방지**: `touches[0]`만 사용, 핀치줌 preventDefault
- **전역 리스너**: document에 mousemove/mouseup 등록 (컴포넌트 밖 드래그 추적)
- **Passive listener**: touchstart/touchmove는 non-passive로 preventDefault 지원
- **메모리 관리**: useEffect cleanup에서 모든 리스너 제거
- **requestAnimationFrame**: 관성 및 자동 회전 모두 RAF 사용
- **타이머 관리**: autoRotateTimeoutId 적절히 clear

---

## 🎨 스타일링 가이드라인

### 컴포넌트가 제공하는 최소 스타일

```css
/* 컨테이너: 3D 컨텍스트만 설정 */
.carousel-container {
  perspective: {perspectiveInRem}rem;  /* 기본: radius * 3.33, 최소 radius * 2 */
  position: relative;
  /* 크기는 부모에 맡김 */
}

/* 회전 래퍼: 3D 변형 유지 */
.carousel-rotator {
  transform-style: preserve-3d;
  transform: rotateY({rotation}deg);
  transition: transform 0s;  /* 드래그 시 즉각 반응 */
}

/* 아이템: 3D 배치만 제어 */
.carousel-item {
  position: absolute;
  width: {itemWidthInRem}rem;
  height: {itemHeightInRem}rem;
  transform: rotateY({itemAngle}deg) translateZ({radiusInRem}rem) scale({calculatedScale});
  opacity: {calculatedOpacity};
  z-index: {calculatedZIndex};  /* -100 ~ 100 범위 */
  /* 비주얼 스타일은 itemClassName으로 커스터마이징 */
}
```

### 소비자가 제어하는 부분

- **컨테이너 크기**: `className`으로 width, height 지정
- **아이템 스타일**: `itemClassName`으로 border, shadow, border-radius, background 등
- **배경, 색상, 타이포그래피**: 완전히 자유
- **레이아웃**: padding, margin 등
- **반응형 breakpoint**: 미디어 쿼리로 자유롭게 조정

### rem 변환 규칙

- **1rem = 16px** 기준
- Props는 px 단위로 받음 (직관적)
- 내부적으로 rem으로 변환하여 스타일 적용
- 예: `radius={600}` → `37.5rem`

---

## 🔄 CI/CD 파이프라인

### GitHub Actions 워크플로우

#### 1. CI (`.github/workflows/ci.yml`)

**트리거**: Push to main/develop, Pull Request
**작업**:

- pnpm 설치 및 의존성 복원
- Biome lint 실행
- TypeScript 타입 체크
- 패키지 빌드 (tsup)

#### 2. Publish (`.github/workflows/publish.yml`)

**트리거**: Push to main (changesets 감지)
**작업**:

- Changesets가 버전 업데이트 PR 생성
- PR 머지 시 자동으로:
  - 패키지 빌드
  - NPM 배포 (`npm publish`)
  - GitHub Release 생성

### NPM Token 설정 방법

1. **NPM에서 Token 생성**

   - https://www.npmjs.com 로그인
   - Profile → Access Tokens → Generate New Token
   - Type: **Automation** 선택
   - Token 복사 (한 번만 표시됨)

2. **GitHub Secrets 등록**

   - Repository → Settings → Secrets and variables → Actions
   - New repository secret
   - Name: `NPM_TOKEN`
   - Value: 복사한 Token
   - Add secret

3. **Scope 설정**
   - NPM에서 `@ddevkim` organization 생성
   - 또는 개인 scope 사용 (username이 자동으로 scope)

---

## 🧪 Playground 구성

### 목적

- 개발 중 실시간 테스트
- 다양한 props 조합 검증
- 성능 프로파일링
- **테스트 전략**: 단위 테스트 생략, Playground로 직접 확인

### 포함할 예시

1. **Basic Usage**: 최소 설정 (이미지 carousel)
2. **Custom Styling**: itemClassName 활용
3. **Auto Rotate**: 자동 회전 + 마우스 인터랙션 데모
4. **Custom Content**: ReactNode 사용 (카드, 비디오 등)
5. **Keyboard Navigation**: 방향키 테스트
6. **Performance**: 30개 아이템 성능 검증
7. **SSR**: Next.js 예시 (별도 예시 프로젝트)

---

## 📋 개발 체크리스트

### Phase 1: 프로젝트 설정

- [ ] Monorepo 구조 생성
- [ ] Root workspace 설정 (pnpm-workspace.yaml)
- [ ] TypeScript 설정 (tsconfig.base.json)
- [ ] Biome 설정
- [ ] Changesets 초기화

### Phase 2: 패키지 구현

- [ ] 상수 정의 (constants.ts)
- [ ] 타입 정의 (types.ts) - Union type으로 명료화
- [ ] 유틸리티 함수 (utils/helpers.ts) - pxToRem, 각도 계산 등
- [ ] 3D 계산 유틸리티 (utils/calculations.ts) - 정규화된 z-index
- [ ] 드래그 핸들러 hook (hooks/useDrag.ts) - 관성, 임계값
- [ ] 자동 회전 hook (hooks/useAutoRotate.ts) - RAF 기반
- [ ] 키보드 네비게이션 hook (hooks/useKeyboard.ts)
- [ ] CarouselCircular 컴포넌트 - 접근성, SSR-safe
- [ ] Public API export (index.ts)

### Phase 3: Playground

- [ ] Vite 설정
- [ ] 기본 예시 작성
- [ ] 다양한 props 조합 테스트

### Phase 4: 빌드 & 배포

- [ ] tsup 설정 (ESM + CJS, external 최적화, best practice)
- [ ] package.json 메타데이터 (버전 명시 금지, peerDependencies)
- [ ] GitHub Actions CI 설정 (lint, type-check, build)
- [ ] GitHub Actions Publish 설정 (changesets 기반)
- [ ] NPM Token 등록 (@ddevkim scope)

### Phase 5: 문서화

- [ ] README.md (설치, 사용법, API, SSR 가이드)
- [ ] CHANGELOG.md (Changesets 자동 생성)
- [ ] JSDoc 주석 (모든 공개 API)
- [ ] 라이선스 (MIT)
- [ ] Next.js SSR 예시 문서

---

## 🚀 배포 워크플로우

### 개발 → 배포 프로세스

```bash
# 1. 기능 개발
git checkout -b feature/new-feature
# ... 코드 작성 ...

# 2. Changeset 생성
pnpm changeset
# - patch: 버그 수정
# - minor: 새 기능
# - major: Breaking changes

# 3. 커밋 & 푸시
git add .
git commit -m "feat: add new feature"
git push

# 4. PR 생성 & 머지
# - CI 자동 실행 (lint, build)
# - main 브랜치 머지

# 5. 자동 배포
# - Changesets가 버전 업데이트 PR 생성
# - PR 머지 시 자동으로 NPM 배포
```

---

## 🎯 성공 지표

### 기술적 목표

- [ ] TypeScript 타입 안전성 100% (strict mode)
- [ ] Tree-shaking 지원 (ESM)
- [ ] 번들 크기 < 50KB (gzipped)
- [ ] React 18+ 호환
- [ ] Next.js SSR 지원
- [ ] 최대 30개 아이템 성능 보장 (60fps)

### 사용성 목표

- [ ] 직관적인 API (5분 내 통합 가능)
- [ ] 명확한 문서화 (SSR 가이드 포함)
- [ ] 최소 스타일 강제 (3D transform만)
- [ ] 다양한 사용 예시 제공 (Playground + Next.js)
- [ ] 접근성 준수 (키보드, ARIA)

---

## 📚 참고 자료 및 설계 원칙

### 구현 시 참고할 핵심 로직

- **3D 변환**: `rotateY()` + `translateZ()` 조합
- **각도 정규화**: 360도 순환 → 0-180 범위로 매핑
- **선형 보간**: 거리 기반 opacity/scale 계산
- **드래그 추적**: 전역 이벤트 리스너 + 관성 애니메이션
- **물리 기반 회전**: 원의 둘레(2πr) 기준 드래그-회전 매핑

### 외부 의존성 최소화

- React hooks만 사용 (외부 라이브러리 지양)
- 순수 CSS 3D transforms
- 브라우저 네이티브 API 활용 (requestAnimationFrame)
- peerDependencies: react, react-dom만

### 핵심 설계 결정 요약

1. **물리 기반**: 드래그는 물리적 원형 구조 기반 (외부 제어 불가)
2. **자동 재개**: 마우스 leave/관성 종료 후 3초 딜레이
3. **멀티터치**: 첫 번째 터치만 추적, 핀치줌 방지
4. **아이템 제한**: 30개 초과 시 slice + warning
5. **SSR-safe**: typeof window 체크, 기본값 제공
6. **접근성 우선**: 키보드 네비게이션, ARIA 속성
7. **타입 안전**: Union type으로 content/image 배타적 정의
8. **성능 최적화**: requestAnimationFrame, useMemo, useCallback
